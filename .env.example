# Copy this file to `.env` and fill in your real credentials. Do NOT commit the real `.env` to the repository.

VITE_SUPABASE_PROJECT_ID=your_project_id
VITE_SUPABASE_PUBLISHABLE_KEY=your_publishable_key
VITE_SUPABASE_URL=https://your-project.supabase.co

# Optional local overrides
# VITE_SUPABASE_URL=https://localhost:54321

# Server-side variables (do NOT add these to client-side .env used in the browser)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
VERTEX_API_KEY=your_vertex_api_key_or_leave_empty_to_use_service_account
VERTEX_PROJECT_ID=your_google_project_id
VERTEX_LOCATION=us-central1
